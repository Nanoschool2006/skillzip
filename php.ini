; cPanel-generated php ini directives, do not edit
; Manual editing of this file may result in unexpected behavior.
; To make changes to this file, use the cPanel MultiPHP INI Editor (Home >> Software >> MultiPHP INI Editor)
; For more information, read our documentation (https://go.cpanel.net/EA4ModifyINI)

; ------------------------------------------------------------
; Stable, growth-ready php.ini (EA-PHP 8.3 / cPanel)
; nanoschool.in — WP + heavy dev activity
; ------------------------------------------------------------

[PHP]
; --- Core behavior ---
allow_url_fopen = On
allow_url_include = Off
enable_dl = Off
expose_php = Off

; --- Error handling (no display on web; log to default cPanel log) ---
display_errors = Off
display_startup_errors = Off
log_errors = On
error_reporting = E_ALL & ~E_DEPRECATED & ~E_NOTICE

; --- Timezone & encoding ---
date.timezone = Asia/Kolkata
default_charset = "UTF-8"

; --- Resource limits (web) ---
; Keep web snappy; long jobs -> wp cron via CLI workers
max_execution_time = 300
max_input_time = 120
memory_limit = 1024M
max_input_vars = 10000

; --- Uploads ---
file_uploads = On
upload_max_filesize = 512M
post_max_size = 512M
max_file_uploads = 50

; --- Output & compression ---
zlib.output_compression = On
output_buffering = 4096

; --- Request parsing order ---
request_order = "GP"
variables_order = "GPCS"

; --- Filesystem perf ---
realpath_cache_size = 16M          ; big WP trees benefit
realpath_cache_ttl  = 900

; --- PCRE ---
pcre.jit = 1

; --- Sockets / SMTP client behavior ---
default_socket_timeout = 20

; --- Sessions (stable, secure, long-lived logins) ---
session.save_handler = files
session.save_path = "/var/cpanel/php/sessions/ea-php83"
session.gc_maxlifetime = 28800
session.gc_probability = 1
session.gc_divisor = 1000
session.cookie_httponly = 1
session.cookie_secure = 1           ; requires HTTPS (recommended)
session.cookie_samesite = Lax
session.use_strict_mode = 1
session.sid_length = 48
session.sid_bits_per_character = 6
session.use_cookies = 1
session.use_only_cookies = 1
session.use_trans_sid = 0
session.lazy_write = 1

; --- Security hardening ---
; If build/deploy tools or WP-CLI need shell access (e.g., ImageMagick),
; remove specific functions from this list cautiously.
disable_functions = show_source,system,passthru,proc_open,popen,dl,symlink,putenv,apache_setenv
; (exec,shell_exec left enabled for dev/CLI workflows; re-disable if not needed)

; --- CGI/FastCGI safety ---
cgi.fix_pathinfo = 0

; ------------------------------------------------------------
; OPcache (web + CLI workers)
; Target: ~3× headroom for codebase growth & frequent deploys
; ------------------------------------------------------------
[opcache]
opcache.enable = 1
opcache.enable_cli = 1
opcache.memory_consumption = 1536       ; 1.5 GB bytecode cache
opcache.interned_strings_buffer = 128
opcache.max_accelerated_files = 200000  ; large codebase & plugins
opcache.enable_file_override = 1        ; faster file_exists/include paths
opcache.validate_timestamps = 1         ; dev-friendly auto reload
opcache.revalidate_freq = 30            ; pick up file changes within 30s
opcache.max_wasted_percentage = 5       ; restart cache sooner if fragmented
opcache.save_comments = 1
opcache.use_cwd = 1
opcache.consistency_checks = 0
; JIT typically hurts WP latency; keep off unless profiled otherwise
opcache.jit = 0
opcache.jit_buffer_size = 0
; Extra safety in multi-user envs (cPanel)
opcache.validate_permission = 1
